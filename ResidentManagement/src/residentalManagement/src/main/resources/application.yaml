server:
  port: 8080
  servlet:
    context-path: /resident-management

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/resident_management"
    username: root
    password: 12345678

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true