jwt:
  signer-key: "FtnneCAPzaRwuVV6JBy1OgtRhoq08Fp/JYu5OFdCj+zPqDyn8+1VlXtwBcX8u4CJ"
  valid-duration: 3600
  refreshable-duration: 7200

server:
  port: 8080
  servlet:
    context-path: /resident-management

spring:
  datasource:
    url: ${DB_URL}
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USER}
    password: ${DB_PASS}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect